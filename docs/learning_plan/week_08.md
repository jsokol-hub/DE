# Week 8

## Main goal
Docker + Phase 2 polish.

## Tuesday — Docker basics
- Install Docker Desktop if not installed
- Create a `docker-compose.yml` with a PostgreSQL container
- Connect to it from Python using the same .env pattern
- Run the cafe pipeline against the local Docker database
- Goal: anyone can clone the repo and run everything locally

## Wednesday — Phase 2 SQL review
- Review all SQL files for consistency: naming, formatting, style
- Make sure all queries run against the Docker database
- Add comments to complex queries explaining the business logic

## Thursday — Phase 2 Python review + month summary
- Clean up Python code: consistent style, no dead code, clear module boundaries
- Run all tests, make sure they pass
- Write a short month review in `docs/reviews/phase_2_review.md`:
  - What was built
  - What was learned
  - What was hardest
  - What to focus on next

## Interview prep (ongoing from this week)
- Practice SQL interview questions: https://roadmapde.roadmappers.ru/content (SQL задачи section)
- Do 2-3 problems per week alongside main work
- Focus on: window functions, CTEs, aggregation edge cases

## Definition of done
- Docker PostgreSQL works and pipeline runs against it
- Code and SQL are clean and consistent
- Month review captures progress honestly
